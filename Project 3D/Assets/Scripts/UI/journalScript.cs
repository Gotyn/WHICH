using UnityEngine;
using UnityEngine.UI;
using System.Collections;

public class journalScript : MonoBehaviour {


    void Update() {
        if (Input.GetKeyDown(KeyCode.P)) {
            //show the journal
            //...
        }
    }

    public void AddJournalEntry(int id, Text entryText) {

    }
}
